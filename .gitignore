# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn-integrity
.pnp.*

# Bun
bun.lockb

# Env
.env
.env.*
!.env.example

# Build outputs
dist/
build/
out/
release/
*.tsbuildinfo

# Exception: the vendored Pretext build IS checked in. Pretext is
# vendored deliberately as Layer 2 insurance (per docs/eng-plan.md),
# and the dist/ files are part of what we vendored — CI should not
# need to rebuild them. Un-ignore vendor/pretext/dist/ specifically.
!vendor/pretext/dist/
!vendor/pretext/dist/**

# Electron-builder
/release-builds/
/dist-electron/

# Test
coverage/
*.lcov
.nyc_output/
playwright-report/
test-results/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS temp
/tmp
*.tmp

# Claude Code local settings + scheduled-task state (never commit)
.claude/

# Logs
logs/
*.log

# Runtime app data (if developer runs the built app locally and app writes here)
/userdata/

# Vendor caches (but commit the vendored Pretext source once added)
# /vendor/.cache/

# Playwright test results + snapshots
playwright-report/
test-results/
/screenshots/

# Vite + Electron dev
.vite/
/out/

# Typecheck cache
*.tsbuildinfo

# Bun cache (but commit bun.lock)
.bun/
